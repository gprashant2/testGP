apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: simple-app
  template:
    metadata:
      labels:
        app: simple-app
    spec:
      containers:
      - name: simple-app
        image: us-central1-docker.pkg.dev/poc-070424/k3s-repo/simple-app:latest
        ports:
        - containerPort: 8080
